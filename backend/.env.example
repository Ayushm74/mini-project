# Google Earth Engine project ID (Cloud project registered with EE)
GEE_PROJECT_ID=your-gcp-project-id

# Optional: path to service account JSON for server-side auth
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account.json
