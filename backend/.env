# Google Earth Engine — use this Cloud project ID with EE API enabled
GEE_PROJECT_ID=absolute-hook-492410-u0

# Optional: service account JSON for server / CI (otherwise use local ee.Authenticate())
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account.json
